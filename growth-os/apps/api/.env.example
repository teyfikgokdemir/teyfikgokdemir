# Growth OS security configuration
# Configure these in production before enabling strict Cloudflare Access JWT validation.
CF_ACCESS_TEAM_DOMAIN=
CF_ACCESS_AUD=

# External mutation stays disabled by default. Do not enable without provider-specific write adapters and approval controls.
EXTERNAL_EXECUTION_ENABLED=false
ADS_WRITE_ENABLED=false
