{
  "name": "second-shepherd",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/apply-consent-fix.mjs && node scripts/remove-footer-marquee.mjs && astro build",
    "audit": "node scripts/audit-built-site.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
