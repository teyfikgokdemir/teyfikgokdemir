---
import { localeNames, locales, type Locale } from '../i18n/locales';
import type { SiteContent } from '../i18n/content';
import { site } from '../data/site';

interface Props {
  locale: Locale | 'fa';
  copy: SiteContent;
  homePath?: string;
  languageUrls?: Partial<Record<Locale | 'fa', string>>;
}

const { locale, copy, homePath, languageUrls } = Astro.props;

const localeUrl = (item: Locale | 'fa') => item === 'tr' ? '/' : `/${item}/`;
const currentHomePath = homePath ?? localeUrl(locale);
const sectionUrl = (section: string) => `${currentHomePath}#${section}`;

const aiVisibilityUrls: Record<Locale | 'fa', string> = {
  en: '/ai-search-visibility/',
  tr: '/yapay-zeka-arama-gorunurlugu/',
  ru: '/ru/ai-search-visibility/',
  sq: '/sq/dukshmeria-ne-kerkimin-ai/',
  mk: '/mk/vidlivost-vo-ai-prebaruvanje/',
  sr: '/sr/vidljivost-u-ai-pretrazi/',
  fa: '/fa/مشاهده-پذیری-در-جستجوی-هوش-مصنوعی/',
  zh: '/zh/ai-search-visibility/',
  vi: '/vi/ai-search-visibility/',
};

const blogUrls: Record<Locale | 'fa', string> = {
  en: '/blog/',
  tr: '/blog/',
  ru: '/ru/blog/',
  sq: '/sq/blog/',
  mk: '/mk/blog/',
  sr: '/sr/blog/',
  fa: '/fa/blog/',
  zh: '/zh/blog/',
  vi: '/vi/blog/',
};

const reflexUrls: Record<Locale | 'fa', string> = {
  en: '/en/reflex/',
  tr: '/reflex/',
  ru: '/ru/reflex/',
  sq: '/sq/reflex/',
  mk: '/mk/reflex/',
  sr: '/sr/reflex/',
  fa: '/fa/reflex/',
  zh: '/zh/reflex/',
  vi: '/vi/reflex/',
};

const reflexUrl = reflexUrls[locale];
const isReflexPage = Astro.url.pathname.includes('/reflex/');
const aiVisibilityUrl = aiVisibilityUrls[locale];
const blogUrl = blogUrls[locale];

const blogLabels: Record<Locale | 'fa', string> = {
  en: 'Insights',
  tr: 'İçgörüler',
  ru: 'Аналитика',
  sq: 'Analiza',
  mk: 'Увид',
  sr: 'Uvidi',
  fa: 'بینش‌ها',
  zh: '商业洞察',
  vi: 'Góc nhìn & Bài viết',
};

const aboutLabels: Record<Locale | 'fa', string> = {
  en: 'About',
  tr: 'Hakkımda',
  ru: 'Обо мне',
  sq: 'Rreth meje',
  mk: 'За мене',
  sr: 'O meni',
  fa: 'درباره من',
  zh: '创始人简介',
  vi: 'Về tôi',
};

const aiVisibilityLabels: Record<Locale | 'fa', string> = {
  en: 'AI Search Visibility',
  tr: 'AI Arama Görünürlüğü',
  ru: 'Видимость в AI поиске',
  sq: 'Dukshmëri AI',
  mk: 'АИ Пребарување',
  sr: 'AI Vidljivost',
  fa: 'دیده‌شدن در هوش مصنوعی',
  zh: 'AI 搜索可见性',
  vi: 'Hiển thị tìm kiếm AI',
};

const nativeLanguageNames: Record<Locale | 'fa', string> = {
  tr: 'Türkçe',
  en: 'English',
  ru: 'Русский',
  mk: 'Македонски',
  sr: 'Srpski',
  sq: 'Shqip',
  fa: 'فارسی',
  zh: '中文',
  vi: 'Tiếng Việt',
};

const blogLabel = blogLabels[locale];
const aboutLabel = aboutLabels[locale];
const aiVisibilityLabel = aiVisibilityLabels[locale];

const navLabels = locale === 'fa'
  ? { ventures: 'ساختارها', expertise: 'تخصص', journey: 'درباره من', contact: 'تماس' }
  : { ventures: copy.nav?.ventures || 'Girişimler', expertise: copy.nav?.expertise || 'Uzmanlık', journey: aboutLabel, contact: copy.nav?.contact || 'İletişim' };

const a11y = locale === 'fa'
  ? { language:'انتخاب زبان', menuOpen:'باز کردن منو', menuClose:'بستن منو' }
  : copy.a11y;

const panelLabels = ({
  tr: { services: 'Hizmetler', approach: 'Uzmanlık', start: 'Birlikte çalışalım', email: 'E-posta gönder' },
  en: { services: 'Services', approach: 'Expertise', start: 'Start a conversation', email: 'Send an email' },
  ru: { services: 'Проекты', approach: 'Подход', start: 'Обсудить задачу', email: 'Написать по email' },
  mk: { services: 'Потфати', approach: 'Пристап', start: 'Да разговараме', email: 'Испрати email' },
  sr: { services: 'Poduhvati', approach: 'Pristup', start: 'Započnimo razgovor', email: 'Pošalji email' },
  sq: { services: 'Sipërmarrjet', approach: 'Qasja', start: 'Nisni një bisedë', email: 'Dërgo email' },
  fa: { services: 'ساختارها', approach: 'رویکرد', start: 'شروع گفتگو', email: 'ارسال ایمیل' },
  zh: { services: '核心业务', approach: '运作体系', start: '开启商务对接', email: '发送商务邮件' },
}[locale] || { services: 'Services', approach: 'Expertise', start: 'Start a conversation', email: 'Send an email' });

const ctsegUrl = locale === 'tr'
  ? 'https://ctseg.com.tr/'
  : locale === 'fa'
    ? 'https://ctseg.com.tr/fa/tamin-beynolmelali-iran/'
    : 'https://ctseg.com.tr/en/';

const ventureSubItems = [
  { name: 'CTSEG', desc: locale === 'tr' ? 'Uluslararası Ticaret ve Tedarik' : locale === 'zh' ? '国际贸易与战略采购' : 'International Trade & Sourcing', url: ctsegUrl, external: true },
  { name: 'QCT Studio', desc: locale === 'tr' ? 'Balkanlar Dijital ve Yazılım' : locale === 'zh' ? '欧洲数字化与技术开发' : 'Balkan Digital & Software', url: 'https://qctstudio.com', external: true },
  { name: 'QCT Commerce', desc: locale === 'tr' ? 'E-Ticaret ve Büyüme Sistemleri' : locale === 'zh' ? '数字化贸易与分销系统' : 'E-Commerce & Digital Growth', url: 'https://qctcommerce.com', external: true },
  { name: 'Reflex Medical', desc: locale === 'tr' ? 'Medikal Tedarik ve İhracat' : locale === 'zh' ? '防护手套供应链与出口' : 'Medical Sourcing & Export', url: reflexUrl, external: false },
];

const expertiseSubItems = [
  { name: locale === 'tr' ? 'Uluslararası Ticaret' : locale === 'zh' ? '国际贸易' : 'International Trade', desc: locale === 'zh' ? '跨境商业运营与落地' : 'Cross-Border Operations', url: sectionUrl('expertise') },
  { name: locale === 'tr' ? 'Stratejik Tedarik' : locale === 'zh' ? '战略采购' : 'Strategic Sourcing', desc: locale === 'zh' ? '供应商核验与 RFQ 比价' : 'Supplier Verification & RFQ', url: sectionUrl('expertise') },
  { name: locale === 'tr' ? 'Dijital Ticaret & Büyüme' : locale === 'zh' ? '数字化贸易与增长' : 'Digital Commerce & Growth', desc: locale === 'zh' ? 'B2B 数字化分销体系' : 'E-Commerce Infrastructure', url: sectionUrl('expertise') },
  { name: aiVisibilityLabel, desc: 'GEO, AEO & AI Search', url: aiVisibilityUrl },
];

const allLocales: (Locale | 'fa')[] = ['tr', 'en', 'ru', 'sq', 'mk', 'sr', 'fa', 'zh'];
---

<header class="site-header">
  <div class="header-container">
    <!-- Brand Logo -->
    <a class="brand" href={currentHomePath} aria-label="Teyfik Gökdemir">
      <picture class="brand__mark">
        <source media="(max-width: 760px)" srcset="/brand/teyfik-gokdemir-monogram.webp" type="image/webp" />
        <source media="(max-width: 760px)" srcset="/brand/teyfik-gokdemir-monogram.png" type="image/png" />
        <source srcset="/brand/teyfik-gokdemir-primary.webp" type="image/webp" />
        <img
          src="/brand/teyfik-gokdemir-primary.png"
          width="705"
          height="203"
          alt="Teyfik Gökdemir"
          decoding="async"
        />
      </picture>
    </a>

    <!-- Hamburger Toggle (Mobile Only) -->
    <button
      class="menu-toggle"
      type="button"
      aria-expanded="false"
      aria-controls="mobile-navigation"
      data-menu-toggle
      aria-label={a11y.menuOpen}
    >
      <span></span><span></span><span></span>
    </button>

    <!-- Desktop Navigation Bar (Desktop Only - UNTOUCHED) -->
    <nav class="desktop-nav" aria-label="Desktop Navigation">
      <div class="desktop-nav-item" data-dropdown>
        <button class="desktop-nav-btn dropdown-trigger" type="button" aria-expanded="false">
          <span>{panelLabels.services}</span>
          <svg width="9" height="5" viewBox="0 0 9 5" fill="none"><path d="M1 1L4.5 4L8 1" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </button>
        <div class="desktop-dropdown-menu dropdown-menu">
          {ventureSubItems.map((item) => (
            <a href={item.url} target={item.external ? '_blank' : undefined} rel={item.external ? 'noopener noreferrer' : undefined} class="desktop-dropdown-link">
              <strong>{item.name}</strong>
              <small>{item.desc}</small>
            </a>
          ))}
        </div>
      </div>

      <div class="desktop-nav-item" data-dropdown>
        <button class="desktop-nav-btn dropdown-trigger" type="button" aria-expanded="false">
          <span>{panelLabels.approach}</span>
          <svg width="9" height="5" viewBox="0 0 9 5" fill="none"><path d="M1 1L4.5 4L8 1" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </button>
        <div class="desktop-dropdown-menu dropdown-menu">
          {expertiseSubItems.map((item) => (
            <a href={item.url} class="desktop-dropdown-link">
              <strong>{item.name}</strong>
            </a>
          ))}
        </div>
      </div>

      <a href={reflexUrl} class="desktop-nav-link" aria-current={isReflexPage ? 'page' : undefined}>Reflex</a>
      <a href={blogUrl} class="desktop-nav-link">{blogLabel}</a>
      <a href={sectionUrl('journey')} class="desktop-nav-link">{aboutLabel}</a>
      <a href={sectionUrl('contact')} class="desktop-nav-link">{navLabels.contact}</a>

      <!-- Desktop Language Dropdown -->
      <div class="desktop-nav-item lang-item" data-dropdown>
        <button class="desktop-nav-btn lang-btn dropdown-trigger" type="button" aria-expanded="false">
          <span>{locale.toUpperCase()}</span>
          <svg width="9" height="5" viewBox="0 0 9 5" fill="none"><path d="M1 1L4.5 4L8 1" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </button>
        <div class="desktop-dropdown-menu lang-menu dropdown-menu">
          {locales.map((item) => (
            <a href={languageUrls?.[item] ?? localeUrl(item)} class:list={['desktop-lang-link', { active: item === locale }]}>
              <span>{nativeLanguageNames[item]}</span>
              <strong>{item.toUpperCase()}</strong>
            </a>
          ))}
        </div>
      </div>
    </nav>

    <!-- Mobile Navigation Panel (Full Screen Modal - z-index 99999) -->
    <div class="mobile-panel" id="mobile-navigation" aria-hidden="true">
      <div class="mobile-panel-head">
        <div class="mobile-panel-brand">
          <span class="mobile-panel-title">teyfikgokdemir</span>
        </div>
        <button class="mobile-panel-close" type="button" aria-label={a11y.menuClose} data-mobile-close>
          <span aria-hidden="true">✕</span>
        </button>
      </div>

      <div class="mobile-panel-body">
        <!-- Mythborn Card 1: DİL SEÇİMİ / LANGUAGE -->
        <div class="myth-card">
          <div class="myth-card-head">
            <span class="myth-card-title">DİL</span>
            <span class="myth-card-badge">GLOBAL</span>
          </div>
          <div class="myth-lang-grid">
            {allLocales.map((item) => (
              <a
                href={languageUrls?.[item] ?? localeUrl(item)}
                lang={item}
                hreflang={item}
                aria-current={item === locale ? 'page' : undefined}
                class:list={['myth-lang-item', { active: item === locale }]}
              >
                <span class="code">{item.toUpperCase()}</span>
                <span class="name">{nativeLanguageNames[item]}</span>
              </a>
            ))}
          </div>
        </div>

        <!-- Mythborn Card 2: GİRİŞİMLER VE YAPILANMA -->
        <div class="myth-card">
          <div class="myth-card-head">
            <span class="myth-card-title">{panelLabels.services.toUpperCase()}</span>
            <span class="myth-card-badge">OPERASYON</span>
          </div>
          <div class="myth-card-grid">
            {ventureSubItems.map((item) => (
              <a href={item.url} target={item.external ? '_blank' : undefined} rel={item.external ? 'noopener noreferrer' : undefined} class="myth-card-item">
                <strong class="item-title">{item.name}</strong>
                <span class="item-desc">{item.desc}</span>
              </a>
            ))}
          </div>
        </div>

        <!-- Mythborn Card 3: UZMANLIK ALANLARI -->
        <div class="myth-card">
          <div class="myth-card-head">
            <span class="myth-card-title">{panelLabels.approach.toUpperCase()}</span>
            <span class="myth-card-badge">STRATEJİ</span>
          </div>
          <div class="myth-card-grid">
            {expertiseSubItems.map((item) => (
              <a href={item.url} class="myth-card-item">
                <strong class="item-title">{item.name}</strong>
                <span class="item-desc">{item.desc}</span>
              </a>
            ))}
          </div>
        </div>

        <!-- Mythborn Card 4: HIZLI ERİŞİM -->
        <div class="myth-card">
          <div class="myth-card-head">
            <span class="myth-card-title">{locale === 'tr' ? 'HIZLI ERİŞİM' : 'NAVIGATION'}</span>
            <span class="myth-card-badge">MENÜ</span>
          </div>
          <div class="myth-card-grid">
            <a href={reflexUrl} class="myth-card-item myth-card-item--highlight" aria-current={isReflexPage ? 'page' : undefined}>
              <strong class="item-title">Reflex</strong>
              <span class="item-desc">Medical Sourcing</span>
            </a>
            <a href={blogUrl} class="myth-card-item">
              <strong class="item-title">{blogLabel}</strong>
              <span class="item-desc">Analysis & Articles</span>
            </a>
            <a href={sectionUrl('journey')} class="myth-card-item">
              <strong class="item-title">{aboutLabel}</strong>
              <span class="item-desc">Background</span>
            </a>
            <a href={sectionUrl('contact')} class="myth-card-item">
              <strong class="item-title">{navLabels.contact}</strong>
              <span class="item-desc">Direct Connect</span>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>

<script is:inline>
  const toggle = document.querySelector('[data-menu-toggle]');
  const panel = document.querySelector('.mobile-panel');
  const closeButton = document.querySelector('[data-mobile-close]');
  let previousFocus = null;
  let previousScroll = 0;

  const focusable = () => panel ? [...panel.querySelectorAll('a[href], button:not([disabled])')].filter(el => !el.closest('[hidden]')) : [];

  const closeMobileMenu = () => {
    if (!panel || !panel.classList.contains('is-open')) return;
    toggle?.setAttribute('aria-expanded', 'false');
    panel.classList.remove('is-open');
    panel.setAttribute('aria-hidden', 'true');
    document.body.classList.remove('has-open-menu');
    window.scrollTo(0, previousScroll);
    if (previousFocus instanceof HTMLElement) previousFocus.focus({ preventScroll: true });
  };

  const openMobileMenu = () => {
    if (!toggle || toggle.getAttribute('aria-expanded') === 'true') return;
    previousScroll = window.scrollY;
    previousFocus = document.activeElement;
    toggle.setAttribute('aria-expanded', 'true');
    panel?.classList.add('is-open');
    panel?.setAttribute('aria-hidden', 'false');
    document.body.classList.add('has-open-menu');
    closeButton?.focus();
  };

  if (toggle && panel) {
    toggle.addEventListener('click', () => {
      const open = toggle.getAttribute('aria-expanded') === 'true';
      if (open) closeMobileMenu();
      else openMobileMenu();
    });
  }

  closeButton?.addEventListener('click', closeMobileMenu);

  // Desktop Dropdowns
  document.querySelectorAll('.desktop-nav [data-dropdown]').forEach((dropdown) => {
    const trigger = dropdown.querySelector('.dropdown-trigger');
    if (!trigger) return;

    trigger.addEventListener('click', (e) => {
      e.stopPropagation();
      const open = trigger.getAttribute('aria-expanded') === 'true';
      document.querySelectorAll('.desktop-nav [data-dropdown] .dropdown-trigger').forEach((other) => {
        if (other !== trigger) other.setAttribute('aria-expanded', 'false');
      });
      trigger.setAttribute('aria-expanded', String(!open));
    });
  });

  document.querySelectorAll('.mobile-panel a').forEach((link) => {
    link.addEventListener('click', () => closeMobileMenu());
  });

  document.addEventListener('click', () => {
    document.querySelectorAll('.desktop-nav [data-dropdown] .dropdown-trigger').forEach((trigger) => {
      trigger.setAttribute('aria-expanded', 'false');
    });
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      document.querySelectorAll('.desktop-nav [data-dropdown] .dropdown-trigger').forEach((trigger) => {
        trigger.setAttribute('aria-expanded', 'false');
      });
      if (panel?.classList.contains('is-open')) closeMobileMenu();
    }
    if (e.key === 'Tab' && panel?.classList.contains('is-open')) {
      const items = focusable();
      if (items.length > 0) {
        const first = items[0];
        const last = items[items.length - 1];
        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      }
    }
  });
</script>

<style is:global>
  body.has-open-menu {
    overflow: hidden !important;
    overscroll-behavior: none !important;
  }

  /* Sticky Desktop Header */
  .site-header {
    height: 80px;
    background: #191512;
    border-bottom: 1px solid rgba(201, 191, 178, 0.15);
    position: sticky;
    top: 0;
    z-index: 110;
    display: flex;
    align-items: center;
  }

  .header-container {
    width: 100%;
    max-width: 1320px;
    margin-inline: auto;
    padding-inline: clamp(1.25rem, 3vw, 2.5rem);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
  }

  .brand__mark img {
    height: 38px !important;
    width: auto !important;
    max-width: 220px !important;
    object-fit: contain !important;
    display: block !important;
  }

  .menu-toggle {
    display: none;
  }

  /* Desktop Navigation Styling (UNTOUCHED & CLEAN) */
  .desktop-nav {
    display: flex;
    align-items: center;
    gap: 1.25rem;
  }

  .desktop-nav-link {
    color: #c9bfb2;
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
    transition: color 180ms ease;
  }

  .desktop-nav-link:hover,
  .desktop-nav-link[aria-current="page"] {
    color: #ffffff;
  }

  .desktop-nav-item {
    position: relative;
  }

  .desktop-nav-btn {
    background: transparent;
    border: none;
    color: #c9bfb2;
    font-size: 0.9rem;
    font-weight: 600;
    padding: 6px 10px;
    border-radius: 8px;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    gap: 5px;
    transition: all 180ms ease;
  }

  .desktop-nav-btn:hover,
  .desktop-nav-btn[aria-expanded="true"] {
    color: #ffffff;
    background: rgba(201, 191, 178, 0.08);
  }

  .desktop-dropdown-menu {
    position: absolute;
    top: calc(100% + 10px);
    left: 0;
    min-width: 260px;
    background: #1c1815;
    border: 1px solid rgba(201, 191, 178, 0.2);
    border-radius: 14px;
    padding: 10px;
    box-shadow: 0 20px 48px rgba(0, 0, 0, 0.5);
    display: none;
    flex-direction: column;
    gap: 4px;
    z-index: 120;
  }

  .desktop-nav-btn[aria-expanded="true"] + .desktop-dropdown-menu {
    display: flex;
  }

  .desktop-dropdown-link {
    display: flex;
    flex-direction: column;
    padding: 8px 12px;
    border-radius: 8px;
    text-decoration: none;
    background: transparent;
    transition: background 150ms ease;
  }

  .desktop-dropdown-link:hover {
    background: rgba(201, 191, 178, 0.1);
  }

  .desktop-dropdown-link strong {
    font-size: 0.88rem;
    color: #ffffff;
  }

  .desktop-dropdown-link small {
    font-size: 0.75rem;
    color: rgba(201, 191, 178, 0.7);
  }

  .lang-item .desktop-dropdown-menu {
    min-width: 180px;
    right: 0;
    left: auto;
  }

  .desktop-lang-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 8px 12px;
    border-radius: 8px;
    color: #c9bfb2;
    text-decoration: none;
    font-size: 0.84rem;
    transition: background 150ms ease, color 150ms ease;
  }

  .desktop-lang-link:hover,
  .desktop-lang-link.active {
    background: rgba(215, 130, 99, 0.2);
    color: #d78263;
  }

  /* Mobile Panel Styling (Standalone Full Screen Modal z-index 99999) */
  .mobile-panel {
    display: none;
  }

  @media (max-width: 1000px) {
    .desktop-nav {
      display: none !important;
    }

    .menu-toggle {
      display: grid !important;
      place-items: center !important;
      width: 44px !important;
      height: 44px !important;
      padding: 8px !important;
      border: 1.5px solid #57483d !important;
      border-radius: 12px !important;
      background: #2a241f !important;
      cursor: pointer !important;
    }

    .menu-toggle span {
      display: block !important;
      width: 20px !important;
      height: 2px !important;
      margin: 2px 0 !important;
      background: #ffffff !important;
      border-radius: 2px !important;
    }

    .mobile-panel {
      position: fixed !important;
      inset: 0 !important;
      z-index: 99999 !important;
      width: 100% !important;
      height: 100dvh !important;
      background: #181512 !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      filter: none !important;
      overflow-y: auto !important;
      overscroll-behavior: contain !important;
      padding: 16px 16px 40px !important;
      display: flex !important;
      flex-direction: column !important;
      gap: 14px !important;
      opacity: 0 !important;
      visibility: hidden !important;
      pointer-events: none !important;
      transform: translateY(-8px) !important;
      transition: opacity 200ms ease, transform 200ms ease, visibility 200ms ease !important;
    }

    .mobile-panel.is-open {
      opacity: 1 !important;
      visibility: visible !important;
      pointer-events: auto !important;
      transform: translateY(0) !important;
    }

    /* ABSOLUTE FORCE COLOR OVERRIDES */
    .mobile-panel,
    .mobile-panel *,
    .mobile-panel a,
    .mobile-panel span,
    .mobile-panel strong,
    .mobile-panel button,
    .mobile-panel div {
      color: #ffffff !important;
    }

    .mobile-panel-head {
      display: flex !important;
      align-items: center !important;
      justify-content: space-between !important;
      padding-bottom: 12px !important;
      border-bottom: 1.5px solid #4a3e35 !important;
    }

    .mobile-panel-brand {
      display: flex !important;
      flex-direction: column !important;
    }

    .mobile-panel-title {
      font-family: Georgia, serif !important;
      font-size: 1.25rem !important;
      font-weight: 700 !important;
      letter-spacing: 0.04em !important;
      color: #ffffff !important;
    }

    .mobile-panel-close {
      width: 48px !important;
      height: 48px !important;
      display: grid !important;
      place-items: center !important;
      border: 1.5px solid #57483d !important;
      border-radius: 12px !important;
      background: #332b24 !important;
      color: #ffffff !important;
      font-size: 22px !important;
      cursor: pointer !important;
    }

    .mobile-panel-body {
      display: flex !important;
      flex-direction: column !important;
      gap: 14px !important;
      width: 100% !important;
    }

    /* Solid Ultra-Bright Cards */
    .mobile-panel .myth-card {
      background: #26201b !important;
      border: 1.5px solid #4a3e35 !important;
      border-radius: 18px !important;
      padding: 16px !important;
      box-shadow: 0 6px 24px rgba(0, 0, 0, 0.45) !important;
    }

    .mobile-panel .myth-card-head {
      display: flex !important;
      align-items: center !important;
      justify-content: space-between !important;
      margin-bottom: 12px !important;
    }

    .mobile-panel .myth-card-title {
      font-size: 0.8rem !important;
      font-weight: 800 !important;
      letter-spacing: 0.12em !important;
      color: #f3eee6 !important;
      text-transform: uppercase !important;
    }

    .mobile-panel .myth-card-badge {
      font-size: 0.7rem !important;
      font-weight: 800 !important;
      padding: 4px 9px !important;
      border-radius: 6px !important;
      background: #d78263 !important;
      color: #ffffff !important;
    }

    .mobile-panel .myth-lang-grid {
      display: grid !important;
      grid-template-columns: repeat(3, 1fr) !important;
      gap: 8px !important;
    }

    .mobile-panel .myth-lang-item {
      display: flex !important;
      flex-direction: column !important;
      align-items: center !important;
      justify-content: center !important;
      padding: 10px 4px !important;
      border-radius: 12px !important;
      border: 1.5px solid #57483d !important;
      background: #332b24 !important;
      color: #ffffff !important;
      text-decoration: none !important;
      text-align: center !important;
    }

    .mobile-panel .myth-lang-item.active {
      background: #d78263 !important;
      border-color: #d78263 !important;
      color: #ffffff !important;
    }

    .mobile-panel .myth-lang-item .code {
      font-size: 0.9rem !important;
      font-weight: 800 !important;
      color: #ffffff !important;
    }

    .mobile-panel .myth-lang-item .name {
      font-size: 0.7rem !important;
      color: #e6dace !important;
      margin-top: 2px !important;
      font-weight: 600 !important;
    }

    .mobile-panel .myth-card-grid {
      display: grid !important;
      grid-template-columns: repeat(2, 1fr) !important;
      gap: 8px !important;
    }

    .mobile-panel .myth-card-item {
      display: flex !important;
      flex-direction: column !important;
      padding: 12px !important;
      border-radius: 12px !important;
      background: #332b24 !important;
      border: 1.5px solid #57483d !important;
      text-decoration: none !important;
      transition: background 150ms ease, border-color 150ms ease !important;
    }

    .mobile-panel .myth-card-item:hover,
    .mobile-panel .myth-card-item:focus-visible {
      background: #443a31 !important;
      border-color: #d78263 !important;
    }

    .mobile-panel .myth-card-item .item-title {
      font-size: 0.95rem !important;
      font-weight: 800 !important;
      color: #ffffff !important;
      line-height: 1.25 !important;
    }

    .mobile-panel .myth-card-item .item-desc {
      font-size: 0.78rem !important;
      color: #e6dace !important;
      margin-top: 3px !important;
      line-height: 1.3 !important;
      font-weight: 600 !important;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .mobile-panel {
      transition: none !important;
    }
  }
</style>
