---
import { localeNames, locales, type Locale } from '../i18n/locales';
import type { SiteContent } from '../i18n/content';
import { site } from '../data/site';

interface Props {
  locale: Locale | 'fa';
  copy: SiteContent;
  homePath?: string;
  languageUrls?: Partial<Record<Locale | 'fa', string>>;
}

const { locale, copy, homePath, languageUrls } = Astro.props;

const localeUrl = (item: Locale | 'fa') => item === 'tr' ? '/' : `/${item}/`;
const currentHomePath = homePath ?? localeUrl(locale);
const sectionUrl = (section: string) => `${currentHomePath}#${section}`;

const aiVisibilityUrls: Record<Locale | 'fa', string> = {
  en: '/ai-search-visibility/',
  tr: '/yapay-zeka-arama-gorunurlugu/',
  ru: '/ru/ai-search-visibility/',
  sq: '/sq/dukshmeria-ne-kerkimin-ai/',
  mk: '/mk/vidlivost-vo-ai-prebaruvanje/',
  sr: '/sr/vidljivost-u-ai-pretrazi/',
  fa: '/fa/مشاهده-پذیری-در-جستجوی-هوش-مصنوعی/',
};

const blogUrls: Record<Locale | 'fa', string> = {
  en: '/blog/',
  tr: '/blog/',
  ru: '/ru/blog/',
  sq: '/sq/blog/',
  mk: '/mk/blog/',
  sr: '/sr/blog/',
  fa: '/fa/blog/',
};

const reflexUrls: Record<Locale | 'fa', string> = {
  en: '/en/reflex/',
  tr: '/reflex/',
  ru: '/ru/reflex/',
  sq: '/sq/reflex/',
  mk: '/mk/reflex/',
  sr: '/sr/reflex/',
  fa: '/fa/reflex/',
};

const reflexUrl = reflexUrls[locale];
const isReflexPage = Astro.url.pathname.includes('/reflex/');
const aiVisibilityUrl = aiVisibilityUrls[locale];
const blogUrl = blogUrls[locale];

const blogLabels: Record<Locale | 'fa', string> = {
  en: 'Insights',
  tr: 'İçgörüler',
  ru: 'Аналитика',
  sq: 'Analiza',
  mk: 'Увид',
  sr: 'Uvidi',
  fa: 'بینش‌ها',
};

const aboutLabels: Record<Locale | 'fa', string> = {
  en: 'About',
  tr: 'Hakkımda',
  ru: 'Обо мне',
  sq: 'Rreth meje',
  mk: 'За мене',
  sr: 'O meni',
  fa: 'درباره من',
};

const aiVisibilityLabels: Record<Locale | 'fa', string> = {
  en: 'AI Search Visibility',
  tr: 'AI Arama Görünürlüğü',
  ru: 'Видимость в AI поиске',
  sq: 'Dukshmëri AI',
  mk: 'АИ Пребарување',
  sr: 'AI Vidljivost',
  fa: 'دیده‌شدن در هوش مصنوعی',
};

const nativeLanguageNames: Record<Locale | 'fa', string> = {
  tr: 'Türkçe',
  en: 'English',
  ru: 'Русский',
  mk: 'Македонски',
  sr: 'Srpski',
  sq: 'Shqip',
  fa: 'فارسی',
};

const blogLabel = blogLabels[locale];
const aboutLabel = aboutLabels[locale];
const aiVisibilityLabel = aiVisibilityLabels[locale];

const navLabels = locale === 'fa'
  ? {
      ventures: 'ساختارها',
      expertise: 'تخصص',
      journey: 'درباره من',
      contact: 'تماس',
    }
  : {
      ventures: copy.nav?.ventures || 'Girişimler',
      expertise: copy.nav?.expertise || 'Uzmanlık',
      journey: aboutLabel,
      contact: copy.nav?.contact || 'İletişim',
    };
const a11y = locale === 'fa'
  ? { language:'انتخاب زبان', menuOpen:'باز کردن منو', menuClose:'بستن منو' }
  : copy.a11y;

const panelLabels = ({
  tr: { services: 'Hizmetler', approach: 'Yaklaşım', start: 'Birlikte çalışalım', email: 'E-posta gönder' },
  en: { services: 'Services', approach: 'Approach', start: 'Start a conversation', email: 'Send an email' },
  ru: { services: 'Проекты', approach: 'Подход', start: 'Обсудить задачу', email: 'Написать по email' },
  mk: { services: 'Потфати', approach: 'Пристап', start: 'Да разговараме', email: 'Испрати email' },
  sr: { services: 'Poduhvati', approach: 'Pristup', start: 'Započnimo razgovor', email: 'Pošalji email' },
  sq: { services: 'Sipërmarrjet', approach: 'Qasja', start: 'Nisni një bisedë', email: 'Dërgo email' },
  fa: { services: 'ساختارها', approach: 'رویکرد', start: 'شروع گفتگو', email: 'ارسال ایمیل' },
}[locale] || { services: 'Services', approach: 'Approach', start: 'Start a conversation', email: 'Send an email' });

const ctsegUrl = locale === 'tr'
  ? 'https://ctseg.com.tr/'
  : locale === 'fa'
    ? 'https://ctseg.com.tr/fa/tamin-beynolmelali-iran/'
    : 'https://ctseg.com.tr/en/';

const ventureSubItems = {
  tr: [
    { name: 'CTSEG', desc: 'Ana Ticaret ve Tedarik', url: ctsegUrl, external: true },
    { name: 'QCT Studio', desc: 'Dijital ve Teknoloji', url: 'https://qctstudio.com', external: true },
    { name: 'QCT Commerce', desc: 'Dijital Ticaret', url: 'https://qctcommerce.com', external: true },
    { name: 'Mythborn', desc: 'Tüketici Markası', url: 'https://mythborn.co', external: true },
  ],
  en: [
    { name: 'CTSEG', desc: 'Principal Trade & Sourcing', url: ctsegUrl, external: true },
    { name: 'QCT Studio', desc: 'Digital & Technology', url: 'https://qctstudio.com', external: true },
    { name: 'QCT Commerce', desc: 'Digital Commerce', url: 'https://qctcommerce.com', external: true },
    { name: 'Mythborn', desc: 'Consumer Brand', url: 'https://mythborn.co', external: true },
  ],
  ru: [
    { name: 'CTSEG', desc: 'Торговля и закупки', url: ctsegUrl, external: true },
    { name: 'QCT Studio', desc: 'Цифровые технологии', url: 'https://qctstudio.com', external: true },
    { name: 'QCT Commerce', desc: 'Цифровая коммерция', url: 'https://qctcommerce.com', external: true },
    { name: 'Mythborn', desc: 'Потребительский бренд', url: 'https://mythborn.co', external: true },
  ],
  mk: [
    { name: 'CTSEG', desc: 'Главна трговија и снабдување', url: ctsegUrl, external: true },
    { name: 'QCT Studio', desc: 'Дигитална технологија', url: 'https://qctstudio.com', external: true },
    { name: 'QCT Commerce', desc: 'Дигитална трговија', url: 'https://qctcommerce.com', external: true },
    { name: 'Mythborn', desc: 'Потрошувачки бренд', url: 'https://mythborn.co', external: true },
  ],
  sr: [
    { name: 'CTSEG', desc: 'Glavna trgovina i nabavka', url: ctsegUrl, external: true },
    { name: 'QCT Studio', desc: 'Digitalne tehnologije', url: 'https://qctstudio.com', external: true },
    { name: 'QCT Commerce', desc: 'Digitalna trgovina', url: 'https://qctcommerce.com', external: true },
    { name: 'Mythborn', desc: 'Potrošački brend', url: 'https://mythborn.co', external: true },
  ],
  sq: [
    { name: 'CTSEG', desc: 'Tregti & Furnizim Kryesor', url: ctsegUrl, external: true },
    { name: 'QCT Studio', desc: 'Teknologji & Digjitale', url: 'https://qctstudio.com', external: true },
    { name: 'QCT Commerce', desc: 'Tregti Digjitale', url: 'https://qctcommerce.com', external: true },
    { name: 'Mythborn', desc: 'Markë Konsumatore', url: 'https://mythborn.co', external: true },
  ],
  fa: [
    { name: 'CTSEG', desc: 'تجارت و تأمین اصلی', url: ctsegUrl, external: true },
    { name: 'QCT Studio', desc: 'فناوری و دیجیتال', url: 'https://qctstudio.com', external: true },
    { name: 'QCT Commerce', desc: 'تجارت دیجیتال', url: 'https://qctcommerce.com', external: true },
    { name: 'Mythborn', desc: 'برند مصرف‌کننده', url: 'https://mythborn.co', external: true },
  ]
}[locale] || ventureSubItems.en;

const expertiseSubItems = {
  tr: [
    { name: 'Uluslararası Ticaret', url: sectionUrl('expertise') },
    { name: 'Stratejik Tedarik', url: sectionUrl('expertise') },
    { name: 'Dijital Ticaret & Büyüme', url: sectionUrl('expertise') },
    { name: aiVisibilityLabel, url: aiVisibilityUrl },
  ],
  en: [
    { name: 'International Trade', url: sectionUrl('expertise') },
    { name: 'Strategic Sourcing', url: sectionUrl('expertise') },
    { name: 'Digital Commerce & Growth', url: sectionUrl('expertise') },
    { name: aiVisibilityLabel, url: aiVisibilityUrl },
  ],
  ru: [
    { name: 'Международная торговля', url: sectionUrl('expertise') },
    { name: 'Стратегические закупки', url: sectionUrl('expertise') },
    { name: 'Цифровая коммерция и рост', url: sectionUrl('expertise') },
    { name: aiVisibilityLabel, url: aiVisibilityUrl },
  ],
  mk: [
    { name: 'Меѓународна трговија', url: sectionUrl('expertise') },
    { name: 'Стратешко снабдување', url: sectionUrl('expertise') },
    { name: 'Дигитална трговија и раст', url: sectionUrl('expertise') },
    { name: aiVisibilityLabel, url: aiVisibilityUrl },
  ],
  sr: [
    { name: 'Međunarodna trgovina', url: sectionUrl('expertise') },
    { name: 'Strateška nabavka', url: sectionUrl('expertise') },
    { name: 'Digitalna trgovina i rast', url: sectionUrl('expertise') },
    { name: aiVisibilityLabel, url: aiVisibilityUrl },
  ],
  sq: [
    { name: 'Tregti Ndërkombëtare', url: sectionUrl('expertise') },
    { name: 'Furnizim Strategjik', url: sectionUrl('expertise') },
    { name: 'Tregti Digjitale & Rritje', url: sectionUrl('expertise') },
    { name: aiVisibilityLabel, url: aiVisibilityUrl },
  ],
  fa: [
    { name: 'تجارت بین‌المللی', url: sectionUrl('expertise') },
    { name: 'تأمین راهبردی', url: sectionUrl('expertise') },
    { name: 'تجارت دیجیتال و رشد', url: sectionUrl('expertise') },
    { name: aiVisibilityLabel, url: aiVisibilityUrl },
  ]
}[locale] || expertiseSubItems.en;

---
<div class="mobile-menu-backdrop" hidden></div>
<header class="site-header">
  <a class="brand" href={currentHomePath} aria-label="Teyfik Gökdemir">
    <picture class="brand__mark">
      <source media="(max-width: 760px)" srcset="/brand/teyfik-gokdemir-monogram.webp" type="image/webp" />
      <source media="(max-width: 760px)" srcset="/brand/teyfik-gokdemir-monogram.png" type="image/png" />
      <source srcset="/brand/teyfik-gokdemir-primary.webp" type="image/webp" />
      <img
        src="/brand/teyfik-gokdemir-primary.png"
        width="705"
        height="203"
        alt="Teyfik Gökdemir"
        decoding="async"
      />
    </picture>
  </a>

  <button
    class="menu-toggle"
    type="button"
    aria-expanded="false"
    aria-controls="site-navigation"
    data-open-label={a11y.menuOpen}
    data-close-label={a11y.menuClose}
    aria-label={a11y.menuOpen}
  >
    <span></span><span></span><span></span>
  </button>

  <div class="header-panel" id="site-navigation">
    <nav aria-label="Main Navigation">
      <div class="nav-item-dropdown" data-dropdown>
        <button class="dropdown-trigger" type="button" aria-expanded="false" aria-haspopup="true" aria-controls={`services-menu-${locale}`}>
          <span>{panelLabels.services}</span>
          <svg class="dropdown-arrow" width="9" height="5" viewBox="0 0 9 5" fill="none" aria-hidden="true">
            <path d="M1 1L4.5 4L8 1" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
        <div class="dropdown-menu" id={`services-menu-${locale}`}>
          {ventureSubItems.map((item) => (
            <a href={item.url} target={item.external ? '_blank' : undefined} rel={item.external ? 'noopener noreferrer' : undefined} class="dropdown-link">
              <span class="dropdown-link__title">{item.name}</span>
              <span class="dropdown-link__desc">{item.desc}</span>
            </a>
          ))}
        </div>
      </div>

      <div class="nav-item-dropdown" data-dropdown>
        <button class="dropdown-trigger" type="button" aria-expanded="false" aria-haspopup="true" aria-controls={`approach-menu-${locale}`}>
          <span>{panelLabels.approach}</span>
          <svg class="dropdown-arrow" width="9" height="5" viewBox="0 0 9 5" fill="none" aria-hidden="true">
            <path d="M1 1L4.5 4L8 1" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
        <div class="dropdown-menu" id={`approach-menu-${locale}`}>
          {expertiseSubItems.map((item) => (
            <a href={item.url} class="dropdown-link">
              <span class="dropdown-link__title">{item.name}</span>
            </a>
          ))}
        </div>
      </div>

      <div class="nav-item-dropdown nav-item-dropdown--contact" data-dropdown>
        <button class="dropdown-trigger" type="button" aria-expanded="false" aria-haspopup="true" aria-controls={`contact-menu-${locale}`}>
          <span>{navLabels.contact}</span>
          <svg class="dropdown-arrow" width="9" height="5" viewBox="0 0 9 5" fill="none" aria-hidden="true">
            <path d="M1 1L4.5 4L8 1" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
        <div class="dropdown-menu contact-menu" id={`contact-menu-${locale}`}>
          <a href={sectionUrl('contact')} class="dropdown-link">
            <span class="dropdown-link__title">{panelLabels.start}</span>
            <span class="dropdown-link__desc">{copy.contact.title}</span>
          </a>
          <a href={`mailto:${site.directEmail}`} class="dropdown-link">
            <span class="dropdown-link__title">{panelLabels.email}</span>
            <span class="dropdown-link__desc">{site.directEmail}</span>
          </a>
          <a href={`https://t.me/${site.telegram}`} class="dropdown-link" target="_blank" rel="noopener noreferrer">
            <span class="dropdown-link__title">Telegram</span>
            <span class="dropdown-link__desc">@{site.telegram}</span>
          </a>
        </div>
      </div>

      <a href={reflexUrl} aria-current={isReflexPage ? 'page' : undefined}>Reflex</a>
      <a href={blogUrl}>{blogLabel}</a>
      <a href={sectionUrl('journey')}>{aboutLabel}</a>
    </nav>

    <div class="nav-item-dropdown lang-dropdown" data-dropdown>
      <button class="dropdown-trigger lang-trigger" type="button" aria-expanded="false" aria-haspopup="true" aria-controls={`language-menu-${locale}`} aria-label={`${a11y.language}: ${locale.toUpperCase()}`}>
        <span class="lang-trigger__code">{locale.toUpperCase()}</span>
        <svg class="dropdown-arrow" width="9" height="5" viewBox="0 0 9 5" fill="none" aria-hidden="true">
          <path d="M1 1L4.5 4L8 1" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </button>
      <div class="dropdown-menu lang-menu" id={`language-menu-${locale}`}>
        {locales.map((item) => (
          <a
            href={languageUrls?.[item] ?? localeUrl(item)}
            lang={item}
            hreflang={item}
            aria-current={item === locale ? 'page' : undefined}
            class:list={['dropdown-link', 'lang-link', { 'is-active': item === locale }]}
            title={localeNames[item]}
          >
            <span class="lang-link__name">{nativeLanguageNames[item]}</span>
            <span class="lang-link__code">{item.toUpperCase()}</span>
          </a>
        ))}
      </div>
    </div>
  </div>
  <div class="mobile-menu-backdrop" data-menu-backdrop aria-hidden="true"></div>
</header>

<script is:inline>
  const toggle = document.querySelector('.menu-toggle');
  const panel = document.querySelector('.header-panel');
  const backdrop = document.querySelector('[data-menu-backdrop], .mobile-menu-backdrop');
  let lastMenuFocus = null;
  let scrollPosition = 0;

  const closeMobileMenu = (restoreFocus = false) => {
    panel?.classList.remove('is-open');
    toggle?.setAttribute('aria-expanded', 'false');
    if (toggle?.dataset?.openLabel) toggle.setAttribute('aria-label', toggle.dataset.openLabel);
    if (backdrop) {
      backdrop.classList.remove('is-visible');
      backdrop.hidden = true;
    }
    document.body.classList.remove('menu-is-open', 'has-open-menu');
    document.body.style.top = '';
    window.scrollTo(0, scrollPosition);
    if (restoreFocus && lastMenuFocus instanceof HTMLElement) lastMenuFocus.focus();
    lastMenuFocus = null;
  };

  const openMobileMenu = () => {
    if (!toggle || toggle.getAttribute('aria-expanded') === 'true') return;
    scrollPosition = window.scrollY;
    lastMenuFocus = document.activeElement;
    toggle.setAttribute('aria-expanded', 'true');
    panel?.classList.add('is-open');
    if (backdrop) {
      backdrop.classList.add('is-visible');
      backdrop.hidden = false;
    }
    document.body.style.top = `-${scrollPosition}px`;
    document.body.classList.add('menu-is-open', 'has-open-menu');
    if (toggle?.dataset?.closeLabel) toggle.setAttribute('aria-label', toggle.dataset.closeLabel);
  };

  if (toggle && panel) {
    toggle.addEventListener('click', () => {
      const open = toggle.getAttribute('aria-expanded') === 'true';
      if (open) {
        closeMobileMenu();
      } else {
        openMobileMenu();
      }
    });
  }

  if (backdrop) {
    backdrop.addEventListener('click', () => closeMobileMenu());
  }

  // Dropdown interactivity
  let lastOpenTrigger = null;
  document.querySelectorAll('[data-dropdown]').forEach((dropdown) => {
    const trigger = dropdown.querySelector('.dropdown-trigger');
    if (!trigger) return;

    trigger.addEventListener('click', (e) => {
      e.stopPropagation();
      const open = trigger.getAttribute('aria-expanded') === 'true';
      
      // Close other dropdowns
      document.querySelectorAll('[data-dropdown] .dropdown-trigger').forEach((other) => {
        if (other !== trigger) other.setAttribute('aria-expanded', 'false');
      });

      trigger.setAttribute('aria-expanded', String(!open));
      lastOpenTrigger = open ? null : trigger;
    });
  });

  document.querySelectorAll('.header-panel a').forEach((link) => {
    link.addEventListener('click', () => closeMobileMenu());
  });

  document.addEventListener('click', () => {
    document.querySelectorAll('[data-dropdown] .dropdown-trigger').forEach((trigger) => {
      trigger.setAttribute('aria-expanded', 'false');
    });
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      document.querySelectorAll('[data-dropdown] .dropdown-trigger').forEach((trigger) => {
        trigger.setAttribute('aria-expanded', 'false');
      });
      closeMobileMenu(true);
      if (lastOpenTrigger) {
        lastOpenTrigger.focus();
        lastOpenTrigger = null;
      }
    }
  });
</script>

<style is:global>
  .mobile-menu-backdrop {
    position: fixed;
    inset: 0;
    z-index: 40;
    background: rgba(0, 0, 0, 0.5);
  }

  body.has-open-menu {
    overflow: hidden;
    overscroll-behavior: none;
    position: fixed;
    width: 100%;
  }

  .site-header {
    height: 86px;
    min-height: 86px;
    padding-inline: clamp(1.5rem, 4vw, 3.5rem);
    background: #211c18;
    border-bottom: 1px solid rgba(201, 191, 178, 0.15);
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: sticky;
    top: 0;
    z-index: 50;
  }
  
  /* Compact branding area */
  .brand {
    display: inline-flex !important;
    align-items: center !important;
    max-width: 230px !important;
    height: auto !important;
    text-decoration: none !important;
  }
  .brand__mark img {
    max-width: 220px !important;
    height: auto !important;
    display: block !important;
    object-fit: contain !important;
  }

  .header-panel {
    display: flex !important;
    align-items: center !important;
    gap: clamp(1.35rem, 2.4vw, 2.5rem) !important;
    overflow: visible !important;
  }
  .header-panel nav {
    display: flex !important;
    align-items: center !important;
    gap: clamp(1rem, 1.8vw, 1.65rem) !important;
  }
  .header-panel nav a,
  .dropdown-trigger {
    font-size: 0.9rem !important;
    font-weight: 500 !important;
    letter-spacing: 0.02em !important;
    color: #c9bfb2 !important;
    background: transparent !important;
    border: none !important;
    cursor: pointer !important;
    text-decoration: none !important;
    transition: color 180ms ease !important;
    display: inline-flex !important;
    align-items: center !important;
    gap: 0.35rem !important;
    padding: 0 !important;
    text-transform: none !important;
  }
  .header-panel nav a:hover,
  .dropdown-trigger:hover,
  .dropdown-trigger[aria-expanded="true"] {
    color: #f3eee6 !important;
  }
  .dropdown-arrow {
    transition: transform 180ms ease !important;
    opacity: 0.7 !important;
  }
  .dropdown-trigger[aria-expanded="true"] .dropdown-arrow {
    transform: rotate(180deg) !important;
  }
  .nav-item-dropdown {
    position: relative !important;
  }
  .dropdown-menu {
    position: absolute !important;
    top: calc(100% + 0.85rem) !important;
    left: 0 !important;
    transform: translateY(8px) scale(.98) !important;
    transform-origin: top left !important;
    min-width: 320px !important;
    display: flex !important;
    padding: .55rem !important;
    background: rgba(33, 28, 24, .98) !important;
    border: 1px solid rgba(201, 191, 178, .22) !important;
    border-radius: 14px !important;
    box-shadow: 0 24px 54px rgba(0, 0, 0, .32), 0 4px 14px rgba(0, 0, 0, .18) !important;
    opacity: 0 !important;
    visibility: hidden !important;
    pointer-events: none !important;
    flex-direction: column !important;
    z-index: 100 !important;
    transition: opacity 180ms ease, transform 180ms cubic-bezier(.2, .75, .25, 1), visibility 180ms ease !important;
  }
  .nav-item-dropdown--contact .dropdown-menu {
    right: 0 !important;
    left: auto !important;
    transform-origin: top right !important;
  }
  .dropdown-trigger[aria-expanded="true"] + .dropdown-menu,
  .nav-item-dropdown:hover .dropdown-menu {
    opacity: 1 !important;
    visibility: visible !important;
    pointer-events: auto !important;
    transform: translateY(0) scale(1) !important;
  }
  @media (hover: hover) and (pointer: fine) {
    .nav-item-dropdown:hover .dropdown-menu {
      opacity: 1 !important;
      visibility: visible !important;
      pointer-events: auto !important;
    }
  }
  .dropdown-link {
    padding: .78rem .85rem !important;
    display: flex !important;
    flex-direction: column !important;
    gap: .24rem !important;
    text-align: left !important;
    border-radius: 9px !important;
    border-bottom: 0 !important;
    transition: background 150ms ease, transform 150ms ease !important;
  }
  .dropdown-link:last-child {
    border-bottom: none !important;
  }
  .dropdown-link:hover,
  .dropdown-link:focus-visible {
    background: rgba(201, 191, 178, .11) !important;
    transform: translateX(2px) !important;
  }
  .dropdown-link__title {
    font-size: .84rem !important;
    font-weight: 750 !important;
    letter-spacing: .01em !important;
    color: #f3eee6 !important;
  }
  .dropdown-link__desc {
    max-width: 28ch !important;
    font-size: .7rem !important;
    font-weight: 400 !important;
    line-height: 1.45 !important;
    color: #a99d8f !important;
  }
  .contact-menu {
    min-width: 390px !important;
    display: grid !important;
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    gap: .3rem !important;
    padding: .65rem !important;
  }
  .contact-menu .dropdown-link:first-child {
    grid-column: 1 / -1 !important;
    padding-bottom: .85rem !important;
    border-bottom: 1px solid rgba(201, 191, 178, .16) !important;
  }
  .contact-menu .dropdown-link:not(:first-child) {
    min-width: 0 !important;
  }
  .contact-menu .dropdown-link__desc {
    max-width: none !important;
    line-height: 1.4 !important;
    overflow-wrap: anywhere !important;
  }
  .dropdown-trigger:focus-visible { outline: 2px solid #d78263 !important; outline-offset: 5px !important; }

  /* Language Selector Dropdown */
  .lang-dropdown {
    position: relative !important;
  }
  .lang-trigger {
    display: inline-flex !important;
    align-items: center !important;
    gap: 0.35rem !important;
    padding: 0.35rem 0.65rem !important;
    font-size: 0.82rem !important;
    font-weight: 600 !important;
    letter-spacing: 0.06em !important;
    color: #f3eee6 !important;
    background: transparent !important;
    border: 1px solid rgba(201, 191, 178, 0.25) !important;
    border-radius: 4px !important;
    cursor: pointer !important;
    transition: all 180ms ease !important;
  }
  .lang-trigger:hover,
  .lang-trigger[aria-expanded="true"] {
    border-color: #a65332 !important;
    color: #f3eee6 !important;
  }
  .lang-menu {
    right: 0 !important;
    left: auto !important;
    transform: none !important;
    min-width: 150px !important;
  }
  .lang-link {
    display: flex !important;
    flex-direction: row !important;
    align-items: center !important;
    justify-content: space-between !important;
    padding: 0.5rem 0.85rem !important;
    text-decoration: none !important;
  }
  .lang-link__name {
    font-size: 0.85rem !important;
    font-weight: 500 !important;
    color: #c9bfb2 !important;
  }
  .lang-link__code {
    font-size: 0.72rem !important;
    font-weight: 600 !important;
    letter-spacing: 0.05em !important;
    color: #8c8275 !important;
  }
  .lang-link:hover .lang-link__name,
  .lang-link.is-active .lang-link__name {
    color: #f3eee6 !important;
  }
  .lang-link.is-active {
    background: rgba(166, 83, 50, 0.2) !important;
  }
  .lang-link.is-active .lang-link__code {
    color: #d78263 !important;
  }
  @media (max-width: 1100px) {
    .brand {
      max-width: 140px !important;
    }
    .brand__mark img {
      max-width: 130px !important;
      max-height: 48px !important;
      width: auto !important;
      height: auto !important;
      object-fit: contain !important;
    }
    .header-panel nav {
      flex-direction: column;
      align-items: flex-start;
      gap: 1.25rem;
    }
    .dropdown-menu {
      position: static !important;
      transform: none !important;
      box-shadow: none !important;
      border: none !important;
      border-radius: 0 !important;
      background: transparent !important;
      padding: .2rem 0 .4rem 1rem !important;
      min-width: 0 !important;
      opacity: 0 !important;
      visibility: hidden !important;
      pointer-events: none !important;
    }
    .dropdown-trigger[aria-expanded="true"] + .dropdown-menu {
      opacity: 1 !important;
      visibility: visible !important;
      pointer-events: auto !important;
    }
        .contact-menu {
      min-width: 0 !important;
      display: flex !important;
      flex-direction: column !important;
      gap: 0 !important;
    }
    .contact-menu .dropdown-link:first-child {
      grid-column: auto !important;
      border-bottom: 1px solid rgba(201, 191, 178, .08) !important;
    }
    .dropdown-link { min-height: 44px !important; }
    .dropdown-link__desc { max-width: 42ch !important; }
    .header-panel nav .nav-item-dropdown { width: 100%; }
    .header-panel nav .dropdown-trigger { justify-content: space-between; width: 100%; min-height: 48px; }
  }
  .mobile-menu-backdrop {
    display: none;
  }
  body.menu-is-open {
    overflow: hidden;
  }

  @media (max-width: 1100px) {
    .site-header {
      position: sticky !important;
      isolation: isolate !important;
    }
    .menu-toggle {
      position: relative !important;
      z-index: 1004 !important;
    }
    .header-panel {
      left: 0 !important;
      right: 0 !important;
      top: calc(100% + 1px) !important;
      width: 100% !important;
      max-height: calc(100dvh - 88px) !important;
      overflow-y: auto !important;
      overscroll-behavior: contain !important;
      display: grid !important;
      gap: .25rem !important;
      padding: .7rem 1rem 1.25rem !important;
      border-radius: 0 0 18px 18px !important;
      z-index: 1003 !important;
      transform: translate3d(0, -10px, 0) !important;
      transition: opacity 180ms ease, transform 220ms cubic-bezier(.23,1,.32,1), visibility 180ms ease !important;
    }
    .header-panel nav {
      display: grid !important;
      gap: .15rem !important;
      width: 100% !important;
    }
    .header-panel nav > a,
    .header-panel nav .dropdown-trigger,
    .lang-trigger {
      width: 100% !important;
      min-height: 48px !important;
      padding: .72rem .75rem !important;
      justify-content: space-between !important;
      border-bottom: 1px solid rgba(201, 191, 178, .14) !important;
    }
    .header-panel nav > a {
      display: flex !important;
    }
    .header-panel nav .nav-item-dropdown,
    .lang-dropdown {
      width: 100% !important;
    }
    .dropdown-menu {
      padding: .25rem 0 .45rem .65rem !important;
      margin: 0 !important;
    }
    .dropdown-link {
      min-height: 44px !important;
      padding: .68rem .75rem !important;
      border-bottom: 1px solid rgba(201, 191, 178, .08) !important;
    }
    .lang-dropdown {
      margin-top: .35rem !important;
      padding-top: .35rem !important;
      border-top: 1px solid rgba(201, 191, 178, .14) !important;
    }
    .mobile-menu-backdrop {
      display: block !important;
      position: fixed !important;
      inset: 87px 0 0 !important;
      z-index: 1001 !important;
      background: rgba(18, 14, 11, .42) !important;
      opacity: 0 !important;
      visibility: hidden !important;
      pointer-events: none !important;
      transition: opacity 180ms ease, visibility 180ms ease !important;
    }
    .mobile-menu-backdrop.is-visible {
      opacity: 1 !important;
      visibility: visible !important;
      pointer-events: auto !important;
    }
    .header-panel.is-open {
      transform: translate3d(0, 0, 0) !important;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .header-panel,
    .mobile-menu-backdrop,
    .dropdown-menu {
      transition: none !important;
    }
  }
</style>
